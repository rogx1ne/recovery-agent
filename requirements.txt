# Python 3.14-compatible pinned versions.
# pydantic-core >= 2.27 ships pre-built cp314 wheels (PyO3 0.23+).
fastapi==0.115.5
uvicorn[standard]==0.32.1
sqlalchemy==2.0.52
razorpay==1.4.2
python-dotenv==1.0.1
pydantic==2.13.4
pydantic-settings==2.15.0
httpx==0.27.2
# Groq SDK for fast AI classification & recovery messages (free tier: 14,400 req/day)
groq>=0.11.0
# setuptools provides pkg_resources (removed from Python 3.14 stdlib); razorpay SDK requires it.
# setuptools >= 71 no longer ships pkg_resources as a top-level module, so pin to <71.
setuptools<71
